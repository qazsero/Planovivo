# Google Cloud Vertex AI Configuration
# Required for image generation with Gemini

# Your Google Cloud Project ID
GOOGLE_VERTEX_PROJECT=your-project-id

# Vertex AI Location (use 'global' for Gemini 3 Pro Image Preview)
GOOGLE_VERTEX_LOCATION=global

# Google Application Credentials
# Option 1 (Local Dev): Use Application Default Credentials (ADC)
#   Run: gcloud auth application-default login
#   Leave this variable unset or comment it out

# Option 2 (CI/CD): Path to service account JSON file
#   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 3 (CI/CD with Secret Manager): Base64-encoded service account JSON
#   GOOGLE_APPLICATION_CREDENTIALS=<base64-encoded-json-string>

# xAI Grok API Configuration
# Required for floorplan validation
# Get your API key from: https://console.x.ai/
XAI_API_KEY=your-xai-api-key-here

# Database Configuration (PostgreSQL)
# Connection string from Railway or other provider
DATABASE_URL="postgresql://user:password@host:port/database"

# PostHog Analytics
# Get these from your PostHog project settings
VITE_POSTHOG_KEY=phc_your_public_key_here
VITE_POSTHOG_HOST=https://eu.i.posthog.com

# Brevo Email Configuration
# Get API key from: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=your-brevo-api-key
BREVO_SENDER_EMAIL=hello@enjinia.es
BREVO_SENDER_NAME=Planovivo

# Session Secret
# Generate a random string for cookie signing
SESSION_SECRET=super-secret-session-key
